| Date | Development Phase / Task Description | Hours Spent |
| :--- | :--- | :--- |
| **8/06** | Environment setup, Python 3.12 installation, runtime configuration, and initial script verification. | 1.0 hr |
| **8/07** | VS Code workspace setup and GitHub repository initialization (`biomechanics-project`) for version control. | 2.0 hrs |
| **8/08** | Integrated model pipeline, executed sliding window segmentation, trained PyTorch CUDA autoencoder, exported anomaly scores, and created introductory README.md. | 3.0 hrs |
| **8/09** | Developed strategic roadmap, injury-prevention framework, and project documentation (hours log, capability files, impact section). | 3.0 hrs |
| **8/10** | Migrated local workspace to VS Code, resolved path resolution bugs (`FileNotFoundError`), and refactored scripts for absolute paths. | 3.0 hrs |
| **8/11** | Built single-subject standalone demo (`demo_skater_a.py`), generated automated visualization outputs, and created markdown documentation. | 2.0 hrs |
| **8/12** | Deployed interactive Streamlit web dashboard (`app.py`) featuring dynamic threshold sliders, metric cards, and CSV export reporting. | 1.0 hr |
| **8/13** | Implemented dynamic statistical thresholding (mean plus two standard deviations) and joint-specific reconstruction error decomposition. Resolved GitHub push conflicts and updated project documentation. | 2.0 hrs |
| **8/14** | Executed synthetic failure stress-testing (`stress_test.py`), configured automated batch multi-file export (`batch_export.py`), and generated time-series multi-joint error heatmaps (`generate_heatmap.py`). | 2.0 hrs |
| **8/15** | Conducted predictive lead-time experiments, resolved MediaPipe C-binding and pathing bugs, and advanced Phase 1 analysis and multi-video ingestion testing. | 3.0 hrs |
| **8/16** | Replaced `moviepy` with OpenCV for video trimming, built ablation and evaluation frameworks, integrated `SkatingDegradationLSTM`, and started debugging fresh vs. fatigued visualization indexing. | 4.0 hrs |
| **8/17** | Optimized pre-deceleration label distributions, tested binary classification thresholds, and documented visualization debugging roadmap. | 1.0 hr |
| **8/18** | Ingested long-form 6-minute time trial video (`skater_time_trial.mp4`), implemented absolute frame mapping for fresh vs. fatigued states, and successfully resolved visualization indexing bugs in `compare_research.py`. | 2.0 hrs |
| **8/19** | Processed Mia Manganello Kilburg's footage, standardized multi-subject file names, and updated the Streamlit dashboard for dynamic skater selection. | 3.0 hrs |
| **8/20** | Added dataset paths, integrated 3000m trial data, attempted `yt-dlp` scraping for Haralds Silovs, and resolved format extraction errors by pivoting to native YouTube embeds. | 3.0 hrs |
| **8/22** | Finalized Phase 1 milestone, structured Phase 2 automation objectives, and updated project capability logs and development journals. | 2.5 hrs |
| **8/23** | Built automated video ingestion engine (`pipeline_engine.py`) and integrated live video auto-digestion into the Streamlit UI (`dashboard.py`). | 2.5 hrs |
| **8/24** | Integrated YouTube URL ingestion support into the pipeline, deployed adaptive stream fallback strategies using `yt-dlp`, debugged FFmpeg dependency and format-restriction blockers, and updated Phase 2 capability roadmap docs. | 1.0 hr |
| **8/25** | Integrated automated baseline calibration, verified end-to-end dashboard telemetry, documented third-party streaming constraints as a core engineering challenge. | 2.0 hrs |
| **8/26** | Attempted PyTorch model quantization (FP32 to INT8) and ONNX export (`skating_model.onnx`); finalized Phase 2 UI polishing and reporting features. | 2.5 hrs |
| **8/27** | Refactored Streamlit dashboard UI state architecture using persistent session state management (`st.session_state`), resolved widget rerun rendering bugs, synchronized Phase 2 capability logs. | 2.5 hrs |
| **8/28** | Standardized execution using Python's `-m` module flag, finalized Phase 2 completion, and synchronized documentation for ACSEF presentation. | 2.0 hrs |
| **8/29** | Conducted exhaustive Phase 1 and Phase 2 documentation sprint, synthesized markdown architecture files, integrated personal athletic and robotics background into project narrative. | 1.5 hrs |
| **8/30** | Resolved Mode 5 Streamlit state-handling bugs and downstream `NameError` exceptions, synchronized project documentation, established the Phase 3 roadmap. | 1.0 hr |
| **8/31** | Resolved schema feature validation errors in `pipeline_engine.py` by aligning checks to the 6 normalized filter features, and verified live stream ingestion resilience. | 2.0 hrs |
| **9/01** | Stabilized pipeline validation rules, aligned 6-feature multivariate inputs, verified end-to-end Streamlit YouTube streaming ingestion workflow. | 1.5 hrs |
| **9/02** | Developed cross-subject normalization and ablation evaluation modules; fixed dashboard `WinError 32` file locks. *(MSE-drop claim in original entry retracted 9/12 — see corrected journal.)* | 2.0 hrs |
| **9/03** | Began multi-view stream synchronization module and PyTorch-to-ONNX export. *(Fusion/latency claims in original entry marked unverified 9/12 — see corrected journal.)* | 1.0 hr |
| **9/04** | Ran a Leave-One-Subject-Out evaluation script. *(100% variance-reduction claim in original entry retracted 9/12 — script found to not test cross-subject generalization and contain a run-blocking bug.)* | 0.5 hrs |
| **9/05** | Tested video rendering pipeline on real-world speed skating footage, analyzed landmark drift and occlusion challenges, planned temporal smoothing upgrades. | 1.0 hr |
| **9/06** | Conducted post-mortem analysis on annotation frame failures and designed upcoming architectural countermeasures (EMA smoothing, confidence gating). | 1.5 hrs |
| **9/07** | Attempted targeted integration of EMA and confidence-score thresholding into `render_robust_annotated_video`; encountered persistent mismappings, marking an ongoing R&D phase. | 1.0 hr |
| **9/08** | Debugged dashboard: root-caused a stray placeholder video file, migrated calibration/rendering code off the removed `mp.solutions.pose` API to the MediaPipe Tasks API, fixed silent `ROOT_DIR`/`sys.path` path-resolution bugs. | 1.0 hr |
| **9/09** | Replaced simulated cross-skater comparison data with a real DTW-based pipeline; corrected mislabeled metrics, added honest CSV/simulated fallback tiers, fixed a threshold-scale mismatch. | 1.0 hr |
| **9/10** | Downloaded and integrated video for 4 additional skaters, expanding real cross-skater comparison coverage to 7 of 10 subjects; fixed a Unicode filename-matching bug. | 1.0 hr |
| **9/11** | Built a Leave-One-Skater-Out ablation script to directly test bone-length scaling's effect on cross-subject generalization; rebuilt mid-run for incremental saving/resumability after losing ~3 hours of unsaved progress. | 1.5 hrs |
| **9/12** | Diagnosed and fixed two real data-quality failures surfaced by the ablation; recorded the real pilot result without further adjustment; audited and corrected prior-session documentation across all capability logs, journal, and hours log after finding several unverifiable/contradicted claims. | 3.5 hrs |
| **9/13** | Built the Phase 3b fatigue-separability ablation (distinct from Phase 3a's general-variance test) and a statistical analysis layer running paired Wilcoxon tests across both experiments. | 1.5 hrs |
| **9/14** | Ran an outlier sensitivity check after noticing one skater dominated every comparison; found the "scaling increases variance" result reverses when that skater is excluded — reported both versions rather than picking one. | 1.0 hr |
| **9/15** | Fixed a real multi-person tracking bug (position + appearance-based identity continuity, EMA smoothing); diagnosed a remaining compilation-footage limitation via a purpose-built diagnostic rather than further guessing; resolved a recurring git divergence caused by parallel local/GitHub-web edits and standardized the documentation workflow. | 1.5 hrs |
| **9/16** | Rebuilt ONNX export/quantization (FP32 verified working, 3.08x measured speedup; INT8 file-size bug fixed but correctness bug found and documented, not claimed as functional). Started Phase 4: built footage audit and start-phase feature engineering, ran first real-footage multi-person tracking test with a confirmed success (correct appearance-based disambiguation) and a new correctly-diagnosed limitation (title-card content, not a tracking bug). | 1.5 hrs |
| **9/18** | Built real Phase 4a tooling (windowed candidate search, manual frame browser, segment comparison script) after automated start-detection failed on real Olympic footage; found and visually confirmed a genuine start-to-acceleration data point with a real (though still caveated) 2.4x velocity increase; caught and corrected a false-positive caused by a broadcast graphic overlay; documented a real camera-cut data gap and two unresolved limitations rather than treating the result as finished. | 1.5 hrs |
| **9/19** | Searched for and manually verified real Olympic start-line footage after automated detection failed on existing footage; built and debugged multiple new diagnostic/comparison tools (manual frame browser, two independent skater-identity checks, technique-phase comparison); resolved the torso-lean identity question decisively; ran and validated the first full-video multi-person tracking test against genuine pack-racing simultaneity. Phase 4a and 4b both completed with real, physically sensible findings. | 1.5 hrs |
| **9/20** | Built structured labeling-log infrastructure (`manage_labeled_segments.py`) and established a rigorous three-step segment-verification process (candidate scan, clean-range identification, identity check). Applied it across 3 videos to grow the Phase 4 reference dataset from n=1 to n=2-3 per technique phase; caught and excluded two contaminated segments that looked clean visually but failed identity verification. | 1.5 hrs |
| **9/21** | Grew the Phase 4 reference dataset to a balanced n=3 across all technique phases; built and applied a leave-one-out sensitivity-testing tool to every phase/metric combination, finding and fixing two real bugs (an incorrect running-average causing inflated variance, and a torso-lean sign-convention artifact from camera angle); tested and correctly retracted a third hypothesis (frame-gap velocity artifact) after direct evidence didn't support it. | 1.5 hrs |
| **9/22** | Fixed the torso-lean sign-convention bug at its source and confirmed it propagates correctly across the full reference table; tested and honestly retracted a ramp-standardization hypothesis (variance increased, not decreased); attempted dual-skater simultaneous tracking through three iterations, diagnosed a real ~21% duplicate-detection cause, and documented the unresolved remainder as future work; built and verified new bilateral (left-side) feature extraction, including a real hip-asymmetry metric. | 2.0 hrs |
| **Total** | **Cumulative Engineering Time** | **83.5 hrs** |